import { AtFavDirectiveDirective } from './at-fav-directive.directive';

describe('AtFavDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AtFavDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
